server:
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:
  config:
    activate:
      on-profile: messaging

logging:
  level:
    org.springframework.boot.context.properties: DEBUG