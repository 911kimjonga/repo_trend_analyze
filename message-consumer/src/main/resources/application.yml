server:
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:
  config:
    activate:
      on-profile: message-consumer

    import:
      - kafka-config.yml

logging:
  level:
    org.springframework.boot.context.properties: DEBUG